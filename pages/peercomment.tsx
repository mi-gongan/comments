import React from "react";
import styled from "styled-components";
import { deleteRecieveComment } from "../src/firebase/firebase";

function peercomment() {
  return <Wrap></Wrap>;
}

export default peercomment;

const Wrap = styled.div``;
